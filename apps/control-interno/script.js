document.addEventListener('DOMContentLoaded',async()=>{const d=await ERP.fetchJSON('data.json',{processes:[],indicators:[]});document.getElementById('processGrid').innerHTML=d.processes.map(p=>`<article class="card process-tile"><div class="list-icon"><i data-lucide="${p.icon}"></i></div><h3>${p.name}</h3><p>${p.description}</p><div class="module-meta"><span class="status status-neutral">${p.status}</span><button class="btn btn-secondary btn-sm" onclick="ERP.toast('Este proceso se desarrollará en la siguiente etapa')">Revisar alcance</button></div></article>`).join('');document.getElementById('indicators').innerHTML=d.indicators.map((i,n)=>`<article class="card metric-card"><div class="metric-copy"><span>${i.label}</span><strong style="font-size:${String(i.value).length>8?'18px':'28px'}">${i.value}</strong><small>${n===0?'Estructura preliminar':'Sin conexión de datos'}</small></div><div class="metric-icon"><i data-lucide="${n===0?'blocks':n===1?'plug':'paintbrush'}"></i></div></article>`).join('');ERP.refreshIcons()});
