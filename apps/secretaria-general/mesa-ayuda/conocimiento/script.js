let D;document.addEventListener('DOMContentLoaded',async()=>{D=await ERP.fetchJSON('data.json',{categories:[],articles:[]});kbCategories.innerHTML=D.categories.map(c=>`<div class="queue-card"><span class="compact-icon"><i data-lucide="${c.icon}"></i></span><strong>${c.count}</strong><span>${c.name}</span></div>`).join('');kbSearch.addEventListener('input',render);render();ERP.refreshIcons()});function render(){const q=kbSearch.value.toLowerCase(),r=D.articles.filter(a=>(a.title+' '+a.category).toLowerCase().includes(q));kbArticles.innerHTML=r.map(a=>`<article class="kb-card"><span class="status ${ITSM.statusTone(a.status)}">${a.status}</span><h3>${a.title}</h3><p>${a.id} · ${a.category} · Actualizado ${ERP.formatDate(a.updated)}</p><footer><span>${a.views} vistas</span><strong>${a.helpful} % útil</strong></footer></article>`).join('');ERP.refreshIcons()}