document.addEventListener('DOMContentLoaded',async()=>{const d=await ERP.fetchJSON('data.json',{settings:[],automations:[]});settingGrid.innerHTML=d.settings.map(s=>`<article class="card"><div class="card-body"><span class="compact-icon"><i data-lucide="${s.icon}"></i></span><h3 style="font-size:13px;margin:12px 0 5px">${s.title}</h3><p style="color:var(--muted);font-size:11px;margin:0">${s.value}</p></div></article>`).join('');automationList.innerHTML=d.automations.map((a,i)=>`<div class="compact-row"><span class="compact-icon"><i data-lucide="workflow"></i></span><div class="compact-main"><strong>${a.name}</strong><span>Si ${a.trigger} → ${a.action}</span></div><button class="filter-chip ${a.active?'active':''}" onclick="this.classList.toggle('active');ERP.toast('Regla actualizada')">${a.active?'Activa':'Inactiva'}</button></div>`).join('');ERP.refreshIcons()});