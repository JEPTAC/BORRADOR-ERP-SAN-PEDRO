let P={data:null,tickets:[]};document.addEventListener('DOMContentLoaded',async()=>{P.data=await ERP.fetchJSON('data.json',{services:[],articles:[]});P.tickets=await ITSM.seed('../shared/seed.json');portalServices.innerHTML=P.data.services.map(s=>`<article class="service-card" onclick="location.href='../tickets/index.html'"><span class="service-icon"><i data-lucide="${s.icon}"></i></span><h3>${s.name}</h3><p>${s.desc}</p><footer><span>${s.type}</span><i data-lucide="arrow-right"></i></footer></article>`).join('');myTickets.innerHTML=P.tickets.slice(0,4).map(t=>`<div class="compact-row"><span class="priority-pill ${ITSM.priorityTone(t.priority)}">${t.priority}</span><div class="compact-main"><strong>${t.title}</strong><span>${t.id} · ${t.status}</span></div><strong class="sla-clock ${ITSM.slaClass(t.resolutionDue)}">${ITSM.remaining(t.resolutionDue)}</strong></div>`).join('');portalArticles.innerHTML=P.data.articles.map(a=>`<div class="compact-row"><span class="compact-icon"><i data-lucide="book-open"></i></span><div class="compact-main"><strong>${a.title}</strong><span>${a.category} · ${a.views} consultas</span></div><i data-lucide="arrow-right"></i></div>`).join('');ERP.refreshIcons()});