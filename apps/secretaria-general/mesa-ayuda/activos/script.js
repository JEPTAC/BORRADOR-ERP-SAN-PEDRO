document.addEventListener('DOMContentLoaded',async()=>{const d=await ERP.fetchJSON('data.json',{metrics:[],assets:[]});assetMetrics.innerHTML=d.metrics.map(m=>`<article class="card metric-card ${m.tone||''}"><div class="metric-copy"><span>${m.label}</span><strong>${m.value}</strong><small>${m.hint}</small></div><div class="metric-icon"><i data-lucide="${m.icon}"></i></div></article>`).join('');assetRows.innerHTML=d.assets.map(a=>`<tr><td><strong class="ticket-code">${a.tag}</strong><span class="subline">${a.name}</span></td><td>${a.type}</td><td>${a.owner}</td><td>${a.office}</td><td><div class="asset-health"><span class="health-dot ${a.health==='Bueno'?'health-good':'health-warn'}"></span>${a.health}</div></td><td>${a.warranty}</td><td><strong>${a.related}</strong> objetos</td></tr>`).join('');ERP.refreshIcons()});